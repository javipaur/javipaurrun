[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x"]

[phases.install]
cmd = "npm ci"

[phases.build]
cmd = "DATABASE_URL='file:./dev.db' npx prisma db push --skip-generate && npm run build"

[start]
cmd = "npm run start"
